name: Python config generator prod

on:
  push:
    branches:
      - 'main'

jobs:
  terraform:
    name: 'Az Function Python deployment'
    runs-on: ubuntu-latest
    steps:
      - name: 'Checkout repository'
        uses: actions/checkout@v5

      - name: Main branch - Prod info
        run: echo "Lack of resources to build Azure Function PROD environment..."
